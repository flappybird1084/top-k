# UI-only dependencies; engine dependencies remain in requirements.txt.
flask==3.1.3
python-dotenv==1.2.3
fastapi==0.141.1
uvicorn==0.52.4
marimo==0.24.2
httpx==0.28.1
# Optional W&B summary metrics (not required for archive or notebook views).
wandb>=0.19
