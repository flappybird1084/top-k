# The one authoritative test runner is pytest (audit finding 34: the unittest
# loader silently skipped the 7 pytest-style files and reported green).
#   uv run python -m pytest tests -q
[pytest]
testpaths = tests
addopts = -q
